# Book table diagnostic (read-only)

Generated: 2026-04-30T04:37:04.296Z

## 1. Total books

**11**

## 2. Each book (id, title, status, ownerId, isPublicDomain)

| id | title | status | ownerId | isPublicDomain |
|----|-------|--------|---------|----------------|
| cmokwxppd00042ws7j9a57wwt | Around the World in Eighty Days | published | null | true |
| cmo3qp0mi0000ycs7aohwk822 | Dracula | published | dev_user_partner | true |
| cmo3qp0u30005ycs7i8ker8ef | Frankenstein | published | dev_user_partner | true |
| cmokwxpqa00052ws7ymp7pi0m | Jane Eyre | published | null | true |
| cmokipnkx000ra8s71u30vfza | Jane Eyre: An Autobiography | published | dev_user_partner2 | false |
| cmokvscix0000mks7xohvzemk | Moby Dick; Or, The Whale | pending_review | dev_user_admin | false |
| cmokwxpka00012ws74rthekys | Pride and Prejudice | published | null | true |
| cmokwxpmo00032ws7yue82lxm | The Adventures of Sherlock Holmes | published | null | true |
| cmokwxpll00022ws7vbikauhq | The Time Machine | published | null | true |
| cmokin5xq0000a8s7l1fsbguj | The Wonderful Wizard of Oz | pending_review | dev_user_admin | false |
| cmokwxpih00002ws7g4425ccf | Treasure Island | published | null | true |

## 3. Books with `ownerId` null

**6**

## 4. Books with status `published`

**9**

## 5. Published books with `ownerId` null

**6**  
Yes — at least one row matches.

## 6. Delete functions for books (codebase survey)

### API routes

- **`DELETE /api/admin/books/[id]`** — `app/api/admin/books/[id]/route.ts`  
  `export async function DELETE`: requires signed-in user; book must exist; caller must be **admin** OR book `ownerId` must equal current user id; then `prisma.book.delete({ where: { id } })`.

### Client components (call the above API)

- **`app/admin/books/[id]/admin-book-detail-client.tsx`** — `deleteBook()` → `fetch(`/api/admin/books/${book.id}`, { method: "DELETE" })`.
- **`app/admin/books/admin-books-client.tsx`** — `deleteBook(book)` → same DELETE URL.
- **`app/(partner)/partner/books/[id]/partner-book-detail-client.tsx`** — `deleteBook()` → same **admin** DELETE route (`/api/admin/books/${book.id}`), not a partner-specific delete route.

There is **no** `DELETE` handler under `app/api/partner/books/[id]/route.ts` in the surveyed file (partner updates use `PATCH` only). Other deletions in the codebase are **chapters**, **chunks**, or related rows during ingest/finalise — not whole-book delete except the admin route above.

## 7. Public `/books` catalogue query

**Server file:** `app/(public)/books/page.tsx`

**Prisma query:**

```ts
const books = await prisma.book.findMany({
  where: { status: "published" },
  orderBy: { title: "asc" },
  select: {
    id: true,
    title: true,
    author: true,
    description: true,
    genre: true,
    coverImageUrl: true,
  },
});
```

**Conditions:**

- DB: **only** `status === "published"`.  
- No filter on `ownerId`, `isPublicDomain`, or other fields at query time.

**Client (`book-catalogue.tsx`):** After load, the UI filters by **genre** only (including “All genres”). Copy on the page refers to “Public-domain works”; that is **not** enforced by this query unless all published rows happen to be public domain.

---

_Report generated by `scripts/book-table-diagnostic.ts` (read-only DB access)._
