/**
 * Single source of truth for profile picker values (onboarding, partner targeting, admin test scenario).
 * Values match Prisma enum strings.
 */
export { AGE_RANGE_OPTIONS as AGE_RANGES, ONBOARDING_AGE_RANGE_OPTIONS } from "@/lib/age-range";
export type { AgeRange } from "@/lib/age-range";
export { GENDER_OPTIONS as GENDERS } from "@/lib/gender";
export type { Gender } from "@/lib/gender";
export { GENRE_OPTIONS } from "@/lib/genre";
